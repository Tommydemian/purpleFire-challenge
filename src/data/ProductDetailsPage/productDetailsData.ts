export type ProductDetail = {
  brand: string;
  model: string;
  availability: string;
};

export const productDetailsData = () => ({
  brand: "LG",
  model: "OLED42C2PSA",
  availability: "Only 2 in Stock",
});
